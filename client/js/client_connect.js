//

/*
$(document).ready(function () {
    console.log(socket.id);
});*/

class Client_connect
{
    constructor()
    {
        //socket_G = io.connect('http://localhost:4000');
        console.log(socket_G.id);
    }
}
